{
  "name": "backend-json",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch dados.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}